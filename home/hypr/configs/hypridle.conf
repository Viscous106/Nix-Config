# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hypridle 
# Original config submitted by https://github.com/SherLock707                                                       
                                                      
$iDIR="$HOME/.config/swaync/images/ja.png"

general {
    lock_cmd = pidof hyprlock || ~/.config/hypr/scripts/hyprlock.sh # runs hyprlock if it is not already running (this is always run when "loginctl lock-session" is called) 
    unlock_cmd = killall -q hyprlock # kills hyprlock when unlocking (this is always run when "loginctl unlock-session" is called)
    before_sleep_cmd = loginctl lock-session    # ensures that the session is locked before going to sleep
    after_sleep_cmd = hyprctl dispatch dpms on # turn of screen after sleep (not strictly necessary, but just in case)
    ignore_dbus_inhibit = false             # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
}

# turn off screen faster if session is already locked
listener {
    timeout = 30                            # 30 seconds
    on-timeout = pidof hyprlock && hyprctl dispatch dpms off # turns off the screen if hyprlock is active
    on-resume = pidof hyprlock && hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
}

# Screensaver - plays video without locking
listener {
#SCREENSAVER_DISABLED# #SCREENSAVER_DISABLED#     timeout = 60                     # 1 minute
#SCREENSAVER_DISABLED# #SCREENSAVER_DISABLED#     on-timeout = ~/.config/hypr/scripts/screensaver.sh # show video screensaver
#SCREENSAVER_DISABLED# #SCREENSAVER_DISABLED#     on-resume = killall -q mpv # stop screensaver on activity
#SCREENSAVER_DISABLED# #SCREENSAVER_DISABLED# }
#SCREENSAVER_DISABLED# 
#SCREENSAVER_DISABLED# # Lockscreen - activates after 3 minutes (screensaver stops)
#SCREENSAVER_DISABLED# listener {
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# #LOCKSCREEN_DISABLED#     timeout = 180                    # 3 minutes
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# #LOCKSCREEN_DISABLED#     on-timeout = killall -q mpv; loginctl lock-session # stop screensaver and lock
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# #LOCKSCREEN_DISABLED# }
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# 
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# # Turn off screen after longer idle
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# listener {
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED#     timeout = 300                            # 5 min
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED#     on-timeout = hyprctl dispatch dpms off  # command to run when timeout has passed
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED#     on-resume = hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
#SCREENSAVER_DISABLED# #LOCKSCREEN_DISABLED# }
#SCREENSAVER_DISABLED# 
#SCREENSAVER_DISABLED# # Suspend # disabled by default
#SCREENSAVER_DISABLED# # listener {
#SCREENSAVER_DISABLED#    # timeout = 1200                            # 20 min
#SCREENSAVER_DISABLED#    # on-timeout = systemctl suspend # command to run when timeout has passed
#SCREENSAVER_DISABLED#     # on-resume = notify-send -i $iDIR " Oh! you're back" "Hello !!!"  # command to run when activity is detected after timeout has fired.
#SCREENSAVER_DISABLED# # }
